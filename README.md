# m2-Data-Augmentation-code
Upload dataset zip to Colab,Extract dataset, Define augmentation operations (with black background), Setup paths and number of augmentations, Loop through all images, apply augmentations, and save results, Add extra custom augmentations, Zip everything and download
